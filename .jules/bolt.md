# 2024-04-24
## Test creation for firebase.ts
- Learning: When writing tests for files that interact with firebase, use `vi.mock` to mock `firebase/app`, `firebase/auth`, and `firebase/firestore`.
- Action: I mocked the respective methods on the firebase modules.
