🧹 [code health improvement] Replace 'any' with appropriate Firestore Timestamp | FieldValue types in src/types.ts

🎯 What: Refactored `src/types.ts` to replace loose `any` types with explicit types. Specifically, `createdAt`, `updatedAt`, and `timestamp` fields were updated to use `Timestamp | FieldValue` from `firebase/firestore`. Additionally, the index signature in `Bot['config']` and the `defaultValue` in `ParameterSchema` were properly constrained. `src/App.tsx` was also updated to align with these stricter types.
💡 Why: Relying on `any` bypasses TypeScript's type-checking, making the codebase more fragile and prone to runtime errors. By using strict types such as `Timestamp | FieldValue`, the compiler can now verify proper usage (e.g., ensuring `toMillis()` isn't mistakenly called on a `FieldValue` object). This significantly improves the maintainability and readability of the codebase without altering functionality.
✅ Verification: Ran `npm run lint` (using `tsc --noEmit`) to ensure there are no compilation errors. Verified changes in `src/App.tsx` where dependent logic was safely adapted to handle the stricter types.
✨ Result: `src/types.ts` is now much more robust, preventing accidental runtime type errors when dealing with Firestore objects and bot configurations.
